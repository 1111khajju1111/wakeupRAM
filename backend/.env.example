# --- Environment ---
ENVIRONMENT=development            # development | test | production
DEBUG=true

# --- Database (Aiven PostgreSQL in production, local Postgres in dev) ---
DATABASE_URL=postgresql+psycopg2://password@pg-6d30a19-bheemasenaa123-2b22.b.aivencloud.com:23916/defaultdb?sslmode=require
DATABASE_SSL_MODE=prefer           # Aiven requires 'require' in production

# --- Auth / Security ---
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30

# --- CORS ---
# Comma-separated list of allowed frontend origins. Never use "*" in production.
CORS_ALLOWED_ORIGINS=http://localhost:5173,capacitor://localhost

# --- Rate limiting ---
RATE_LIMIT_DEFAULT=100/minute

# --- AI provider (never hardcode keys, never expose to frontend) ---
AI_PROVIDER=groq
GROQ_API_KEY=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
AI_MODEL_NAME=openai/gpt-oss-120b
AI_MAX_TOKENS=1024
AI_DEFAULT_MODE=adaptive
AI_MEMORY_RETRIEVAL_LIMIT=20

# --- Voice (STT/TTS provider, filled in during Phase 9) ---
STT_PROVIDER=google
STT_MODEL_NAME=gemini-3.5-transcribe-live
TTS_PROVIDER=google
TTS_MODEL_NAME=gemini-3.1-flash-tts-preview
GOOGLE_API_KEY=AQ.Ab8RN6K0hCnCaThjRPYO0S_s8pCgMv6h9HLKI2WFozX5Js0DGQ

# --- Deployment metadata ---
BACKEND_BASE_URL=http://localhost:8000
FRONTEND_BASE_URL=http://localhost:5173
